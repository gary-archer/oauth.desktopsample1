/*
 * Constants for named events
 */
export class ApplicationEventNames {

    // Used to being the app to the foreground when a login completes
    public static readonly ON_LOGIN_REACTIVATE = 'login_reactivate';
}
